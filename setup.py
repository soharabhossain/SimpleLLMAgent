# Run the following commad at the terminal
# > pip install -e .


from setuptools import setup, find_packages

setup(
    name='simple_llm_agent',
    version='0.1',
    packages=find_packages(),
)
