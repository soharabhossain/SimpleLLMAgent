# Run the following commad at the terminal
# > pip install -e .


from setuptools import setup, find_packages

setup(
    name='simple_llm_agent',
    version='0.1',
    description="A simple LLM-based agent",
    author="Soharab Hossain Shaikh",
    packages=find_packages(),
)
